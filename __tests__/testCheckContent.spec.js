import { checkContent } from "../src/client/js/checkContent";

describe("Testing the checkcontent functionality", () => {
    test("Testing the checkContent() function", () => {
        expect(checkContent).toBeDefined();
})});